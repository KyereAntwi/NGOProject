@page
@model AppUi.Pages.BlogsModel

@{
    ViewData["Title"] = "Blogs";
    Layout = "_Layout";
}

<div style="background-color:mediumvioletred; color: white; height:300px">
    <div class="container">
        <div class="row">
            <div class="col align-content-center">
                <h1 style="font-family:Verdana, Geneva, Tahoma, sans-serif; font-size:5rem;">BLOG LIST</h1>
            </div>
        </div>
    </div>
</div>

<div class="container mt-4">
    <div class="row">
        @if (Model.ViewModel.Blogs.Count > 0)
        {
            foreach (var item in Model.ViewModel.Blogs)
            {
                <div class="col">
                    <div class="card d-block mr-2" style="width:18rem">
                        <img class="card-img-top" src="@Url.Page("/BlogImage", new { Id = item.Id })"/>
                        <div class="card-body">
                            <h4 class="card-title">@item.Title</h4>
                            <h6 class="card-subtitle">@item.DateAdded.ToShortDateString()</h6>
                            <p class="text-wrap">@item.Introduction</p>
                        </div>
                        <div class="card-footer">
                            <a class="card-link" href="@Url.Page("/Blog", new { Id = item.Id })">View Details</a>
                        </div>
                    </div>
                </div>
            }
        }
        else 
        {
            <div class="col-12">
                <h1 style="text-align:center;">There are currently no Blogs published yet</h1>
            </div>
        }
    </div>
</div>
